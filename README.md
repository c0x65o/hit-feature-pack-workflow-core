# @hit/feature-pack-workflows

A HIT feature pack

## Installation

```bash
hit feature add workflows
```

Or manually:
1. Add to `hit.yaml` under `feature_packs:`
2. Add `"@hit/feature-pack-workflows": "github:c0x65o/hit-feature-pack-workflows"` to `package.json`
3. Add `'@hit/feature-pack-workflows'` to `hitPackages` array in `next.config.js`
4. Run `npm install`
5. Run `hit run` to generate routes

## Development

```bash
npm install
npm run build
```
